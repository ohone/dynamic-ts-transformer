{
  "name": "dynamic-ts-transformer",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "oldbuild": "tsc -p tsconfig.scripts.json && node scripts/build-types.js && tsc",
    "build": "tsc",
    "dev": "NODE_ENV=development tsc && node dist/dev.js",
    "test": "npm run test"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "typescript": "^5.6.3"
  },
  "devDependencies": {
    "@types/node": "^22.8.1"
  }
}
